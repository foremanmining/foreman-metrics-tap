package mn.foreman.metricstap;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Verifies that the context loads. */
@SpringBootTest
class MetricsTapApplicationTests {

    /** Verifies that the context loads. */
    @Test
    void contextLoads() {
        // Do nothing
    }
}
